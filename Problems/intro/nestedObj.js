//create an object inside another object with at least 1 key:value in both objects then access that nested object's key:value
let object1 = {
  str: "Hey",
  object2: {
    num: 3
  }
}
object1.object2
