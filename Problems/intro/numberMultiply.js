// create a for loop when given a interger in variable num, it multiplies all of the factorials of that number
let solution = 1;

for (let num = 12; num >= 1; num = num - 1) {
        solution = solution * num
}
console.log(solution);
